{
  "name": "analytics-platform",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "docs:claude": "bash scripts/refresh-claude-docs.sh"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "workspaces": ["apps/*", "packages/*"]
}
